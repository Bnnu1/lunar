# Lunar
Simple AUR helper written in Rust  
Supports multi-package install
To install (You will need Rust installed):  
```
git clone https://github.com/Bnnu1/lunar
cd lunar
cargo build --release
sudo cp target/release/lunar /usr/local/bin/
```  
To run:  
```
lunar [any number of packages to search for]
```
